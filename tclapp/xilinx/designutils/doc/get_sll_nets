

  Description: 

     Get routed inter-SLR nets that have a VLONG12 cost and over 100 wire shapes on at least one of its nodes.

     This command must be run on an implemented design.

  Example:
  
     set nets [::tclapp::xilinx::designutils::get_sll_nets]
     set nets [::tclapp::xilinx::designutils::get_sll_nets [get_nets ingressLoop[0].ingressFifo/reset_*]]

