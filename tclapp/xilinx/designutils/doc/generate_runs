

  Description: 

     Create all possible combinations of runs.

     This command must be run on a synthesized or implemented design.

  Example:
  
    The following example generate all possible combination of runs (phys_opt_design is not run):
     ::xilinx::designutils::generate_runs 0

    The following example generate all possible combination of runs (phys_opt_design is run):
     ::xilinx::designutils::generate_runs 1
